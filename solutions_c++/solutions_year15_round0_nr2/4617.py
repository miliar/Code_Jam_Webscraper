#include <stdio.h>#include <queue>#include <vector>#include <stdlib.h>#include <iostream>using namespace std;int mini(int a, int b){	if (a>b) return b;	else return a;}int main(){	int c;	cin >> c;	for (int ii = 0; ii< c; ii++){		int d, p[1001], time = 0, ans = 10001;		priority_queue<int> t;		cin >> d;		for (int i = 0; i<d; i++) cin >> p[i];		for (int i = 1; i < 1000; i++)		{			int tmp = 0;			for (int j = 0; j < d; j++) {				if (p[j] % i == 0)				{					tmp += p[j] / i-1;				}				else tmp += (p[j] - p[j] % i) / i;			}			ans = mini(ans, tmp + i);		}		printf("Case #%d: %d\n", ii + 1, ans);	}	return 0;}