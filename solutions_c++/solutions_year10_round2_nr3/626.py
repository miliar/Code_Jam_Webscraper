#include <cstdio>
#include <string>
#include <vector>
#include <iostream>

using namespace std;

typedef unsigned int uint;
typedef long long ll;
typedef unsigned long long ull;

#define All(a) (a).begin(),(a).end()
#define pb push_back
#define mp make_pair
#define sz(a) int((a).size())
#define Sort(c) sort((c).begin(),(c).end())

int main(int argc, char* argv[])
{
	uint N_;
	cin >> N_;
	
	for(uint n_=0;n_<N_;n_++)
	{
		uint M_;
		cin >> M_;
		vector<> v(M_);
		for(uint m_=0;m_<M_;m_++)
		{
			cin >> v[i];
		}
		r;
		cout << "Case #" << n_+1 << ": " << r << endl;
	}
	
	return 0;
}
