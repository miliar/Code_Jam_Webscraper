#include <bits/stdc++.h>#define rep(i,a,b) for(int i=(int)a;i<=(int)b;++i)using namespace std;#define pb push_back#define mp make_pair#define sd(x) scanf("%d",&x)#define ss(x) scanf("%s",x)#define sl(x) scanf("%lld",&x)#define SZ(x) (int)x.size()#define ALL(x) x.begin(),x.end()const int N = 1e5+10,mod = 1e9+7;typedef long long ll;int n,pos[N],sp[N],d;bool check(double mid){	bool f = 1;	rep(i,1,n){		f &= (d*1.0/mid > (d-pos[i])*1.0/sp[i]);	}	return f;}int main() {	int T;	sd(T);	rep(i,1,T){		printf("Case #%d: ", i);		sd(d),sd(n);		rep(i,1,n){			sd(pos[i]);sd(sp[i]);		}		double l=0,r=1e17;		rep(i,1,400){			double mid = (l+r)/2;			if(check(mid)){				l = mid;			} else r = mid;		}		printf("%.6f\n", l);	}}