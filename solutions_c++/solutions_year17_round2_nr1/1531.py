#include <algorithm>#include <bitset>#include <cassert>#include <deque>#include <queue>#include <iomanip>#include <iostream>#include <iterator>#include <float.h>#include <limits>#include <list>#include <map>#include <math.h>#include <numeric>#include <set>#include <sstream>#include <stack>#include <stdio.h>#include <string>#include <vector>using namespace std;typedef long long ll;typedef unsigned long long ull;typedef pair<int, int> pi;typedef pair<ll, ll> pl;typedef vector<bool> vb;typedef vector<int> vi;typedef vector<unsigned> vu;typedef vector<ll> vl;typedef vector<pi> vp;typedef vector<string> vs;typedef set<int> si;typedef map<int, int> mi;int oi; int oj;int main(){    std::set<long long>::iterator it;    int t;    cin >> t;    for(int Case=1; Case<=t; Case++){        long N;        double D;        cin >> D >> N;        double hi=100000.2, lo=10000.2;        double tMax = 0;        std::vector< double > d(N);        std::vector< double > s(N);        for(long i=0; i<N; i++){        	cin >> d[i] >> s[i];        	tMax = max( (D-d[i])/s[i], tMax );        }        lo = D/tMax;        cout  << "Case #" << Case << ": ";        printf("%.6f", lo);        cout << endl;    }    return 0;}