#include <stdio.h>
#include <iostream>
#include <vector>
#include <set>
#include <map>
#include <string>
#include <algorithm>

using namespace std;

int main()
{
    scanf("%d",&t);
	while (t--)
	{
	    scanf("%d",&n);
	    nb=1;
	    no=1;
	    for (i=0; i<n; i++)
	    {
	        cin>>s>>a;
	        if (s=="o")
	        {
	            ans+=(a-nb)+1;
	        }
	        else
	return 0;
}
