#include <iostream>#include <stdio.h>#include <string.h>#include <stdlib.h>#include <vector>#include <algorithm>#include <math.h>using namespace std;//Assume Kaylin could eat any number of mushroom pieces at any time.int m1 (vector<int> m, int n) {  int y = 0;  for (int i=1; i<n; ++i) {    if (m[i]<m[i-1]) {      y += m[i-1]-m[i];    }  }  return y;}// Assume that, starting with the first time we look at the plate, Kaylin eats// mushrooms at a constant rate whenever there are mushrooms on her plate.int m2 (vector<int> m, int n) {  int z = 0;  int maxInterval = 0;  for (int i=1; i<n; ++i) {    maxInterval = max(maxInterval, (m[i-1]-m[i]));  }  //cout << " Max Interval: " << maxInterval << endl;  for (int i=0; i<n-1; ++i) {    //if (m[i]<m[i+1]) {    if (m[i]<maxInterval) {      z += m[i];    } else {      z += maxInterval;    }  }  return z;}int main() {  int T;  cin >> T;  for (int x=1; x<=T; ++x) {    // Input    int N;    cin >> N;    vector<int> mushrooms(N);    for (int n=0; n<N; ++n) {      int mi;      cin >> mi;      mushrooms[n] = mi;    }    // Solve    int y = m1(mushrooms,N);    int z = m2(mushrooms,N);    // Output    printf("Case #%i: %i %i\n", x, y, z);  }  return 0;}