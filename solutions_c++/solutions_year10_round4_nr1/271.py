#include <stdio.h>
#include <algorithm>
#include <vector>
#include <set>

using namespace std;

int matr[1000][1000];

int main()
{
	//freopen("out.txt", "w", stdout);
	//freopen("A-small-attempt0.in", "r", stdin);
	int T;
	scanf("%d", &T);
	for (int t = 0; t < T; t++)
	{		
		double res;
		int K;
		scanf("%d",&K);
		for (int i = 0; i < 2*k+1)
			for
		
		
		
	}
	//fclose(stdout);
	//fclose(stdin);

	return 0;
}