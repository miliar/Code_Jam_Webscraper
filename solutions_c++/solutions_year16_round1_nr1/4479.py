#include <iostream>#include <string>#include <vector>using namespace std;class solution{	string str;public:	solution(string in){		for(int i=0;i<in.size();i++){			if(i!=0 && in[i]>=in[i-1] && in[i]>=str.front()){				str.insert(str.begin(),in[i]);			}			else{				str.push_back(in[i]);			}		}		cout << str << endl;	}};int main(){	int count;	cin >> count;	for(int i=0;i<count;i++){		string tmp;		cin >> tmp;		cout << "Case #" << i+1 << ": ";		solution a(tmp);	}}