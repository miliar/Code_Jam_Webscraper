#include <cstdio>#include <cstring>int main() {		int cases;	double C, F, X;	scanf(" %d", &cases);	for (int casei = 1; casei <= cases; ++casei) {		scanf("%lf%lf%lf", &C, &F, &X);		double ans = X * 0.5;		int A = 0;		double t = 0;		while (true) {			if (t + X/(2+A*F) < ans) ans = t + X/(2+A*F);			t += C / (2 + A * F);			if ((X-C)/(2+A*F) <= C/(2+(A+1)*F)) break;			++A;			if (A > 100000000) break;		}		printf("Case #%d: %.8f\n", casei, ans);	}		return 0;}