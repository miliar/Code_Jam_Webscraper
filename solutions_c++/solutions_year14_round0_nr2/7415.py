#include <stdio.h>int main(){  int T, i, flag;  double C, F, X, v, t;  scanf("%d\n", &T);  for (int i = 1; i <= T; i++) {    scanf("%lf %lf %lf\n", &C, &F, &X);    t = 0.0;     v = 2.0;    flag = 1;    while (flag) {      double rtime = X / v;      double btime = C / v + X / (v + F);      if (btime < rtime) {        t += C / v;        v += F;      } else {        t += rtime;        flag = 0;      }    }    printf("Case #%d: %.7lf\n", i, t);  }  return 0;}