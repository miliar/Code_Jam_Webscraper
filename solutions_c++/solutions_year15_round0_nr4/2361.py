#include <iostream>
#include <stdio.h>
#include <stack>
#include <queue>
#include <string>
#include <algorithm>
#include <vector>
using namespace std;
int main()
{
	freopen("src.txt", "r", stdin);
	freopen("out.txt", "w", stdout);
	int tt, x, r, c;
	cin >> tt;
	for (int t = 1; t <= tt; t++)
	{
		cin >> x >> r >> c;
	}
	return 0;
}