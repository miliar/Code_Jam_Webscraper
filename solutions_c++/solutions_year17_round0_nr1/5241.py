#include <bits/stdc++.h>#define N (1<<18)#define ll long long#define ld long double#define M 1000000007#define INF 0x5ADFACE5#define LINF 0x51DEEFFEC7C0DECALL#define pii pair<int, int>#define pll pair<long long, long long>#define F first#define S secondusing namespace std;int main () {	int n;	cin>>n;	for (int i = 0; i < n; i++) {		string s;		int k;		cin>>s>>k;		int ans = 0;		for (int i = 0; i <= s.length() - k; i++) {			if (s[i] == '-') {				for (int j = i; j < i + k; j++) {					if (s[j] == '-') s[j] = '+';					else s[j] = '-';				}				ans++;			}		}		bool b = true;		for (char c : s) {			if (c == '-') b = false;		}		if (b) cout<<"Case #"<<i+1<<": "<<ans<<endl;		else cout<<"Case #"<<i+1<<": "<<"IMPOSSIBLE"<<endl;	}}