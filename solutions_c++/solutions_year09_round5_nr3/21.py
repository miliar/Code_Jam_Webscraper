#include <iostream>
#include <sstream>
#include <algorithm>
#include <cmath>
#include <vector>
#include <map>
#include <set>
#include <list>
#include <deque>

using namespace std;

int	main()
{
	int task;
	scanf("%d", &task);
	for (int now_case = 0; now_case < task; ++now_case)
	{
	}
	return 0;
}
