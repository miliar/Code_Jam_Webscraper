#include <bits/stdc++.h>#define N (1<<18)#define ll long long#define ld long double#define M 1000000007#define INF 0x5ADFACE5#define LINF 0x51DEEFFEC7C0DECALL#define pii pair<int, int>#define pll pair<long long, long long>#define F first#define S secondusing namespace std;int main () {	int t;	cin>>t;	for (int i = 1; i <= t; i++) {		int n, k;		cin>>n>>k;		int v[n + 2];		for (int i = 0; i <= n + 1; i++) v[i] = 0;		v[0] = v[n + 1] = 1;		int bl = 0;		int br = 0;		for (int i = 0; i < k; i++) {			int bi = 1;			bl = 0;			br = 0;			for (int j = 1; j <= n; j++) {				int l = -1;				int r = -1;				for (int k = j; !v[k]; k--) {					l++;				}				for (int k = j; !v[k]; k++) {					r++;				}				int mx = min(l, r);				if (mx > bl) {					bi = j;					bl = min(l, r);					br = max(l, r);				} else if (mx == bl && br < max(l, r)) {					bi = j;					bl = min(l, r);					br = max(l, r);				}			}			v[bi] = 1;		}		cout<<"Case #"<<i<<": "<<br<<" "<<bl<<endl;	}}