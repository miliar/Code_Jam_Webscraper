#include <cstdio>#include <cstring>int main() {		int cases, row, num;	bool number[17];	scanf(" %d", &cases);	for (int casei = 1; casei <= cases; ++casei) {		memset(number, true, sizeof number);		for (int i = 0; i < 2; ++i) {			scanf(" %d", &row);			for (int j = 0; j < 4; ++j)				for (int k = 0; k < 4; ++k) {					scanf(" %d", &num);					if (j != row - 1) number[num] = false;				}		}		int ans = -1;		for (int j = 1; j <= 16; ++j)			if (number[j]) {				if (ans == -1) ans = j;				else {					ans = -2;					break;				}			}		switch (ans) {			case -1:				printf("Case #%d: Volunteer cheated!\n", casei);				break;			case -2:				printf("Case #%d: Bad magician!\n", casei);				break;			default:				printf("Case #%d: %d\n", casei, ans);		}	}		return 0;}