#include <bits/stdc++.h>#define N (1<<18)#define ll long long#define ld long double#define M 1000000007#define INF 0x5ADFACE5#define LINF 0x51DEEFFEC7C0DECALL#define pii pair<int, int>#define pll pair<long long, long long>#define F first#define S secondusing namespace std;int main () {	int n;	cin>>n;	ld pi = -4 * atan(-1);	for (int i = 1; i <= n; i++) {		int m, k;		cin>>m>>k;		pair<ld, ld> p[m];		for (int i = 0; i < m; i++) {			cin>>p[i].S>>p[i].F;			p[i].F *= p[i].S;		}		sort(p, p + m);		reverse(p, p + m);		ld ans = 0;		for (int i = 0; i < m; i++) {			ld t = pi * p[i].S * p[i].S + 2 * pi * p[i].F;			int l = k - 1;			for (int j = 0; j < m; j++) {				if (!l) break;				if (i != j && p[j].S <= p[i].S) t += 2 * pi * p[j].F, l--;				if (!l) break;			}			ans = max(ans, t);		}		cout<<"Case #"<<i<<": "<<setprecision(30)<<ans<<endl;	}}