#include <cstdio>#include <cstring>#include <algorithm>using namespace std;int calc(double *b1, double *b2, int N) {	int res = 0;	for (int i = 0, j = 0; i < N; ++i)		if (b1[i] > b2[j]) {			++res;			++j;		}	return res;}int main() {	double block[2][2000];		int cases;	scanf(" %d", &cases);	for (int casei = 1; casei <= cases; ++casei) {		int N;		scanf(" %d", &N);		for (int i = 0; i < N; ++i) scanf(" %lf", block[0] + i);		for (int i = 0; i < N; ++i) scanf(" %lf", block[1] + i);				sort(block[0], block[0] + N);		sort(block[1], block[1] + N);						int ans1 = calc(block[0], block[1], N), ans2 = N - calc(block[1], block[0], N);		printf("Case #%d: %d %d\n", casei, ans1, ans2);	}		return 0;}