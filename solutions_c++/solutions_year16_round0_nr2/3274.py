#include <cstdio>
#include <cstring>

using namespace std;

int main() {
	int T;
	scanf("%d", &T);
	bool vis[10] = {false};
	int dcnt = 0;
	for (int tn = 1; tn <= T; tn++) {
		int n;
		scanf("%d", &n);
		memset(vis, 0, sizeof(vis));
		dcnt = 0;
		
	}
} 