#include <iostream>#include <list>using namespace std;int main(){	int cases, buttons;	cin >> cases;	for(int i=0; i<cases; i++){		cin >> buttons;		list<int> oarray, barray;		list<char> moves;		for(int j=0; j<buttons;j++){			char letter;			int number;			cin >> letter;			cin >> number;			moves.push_back(letter);			if(letter == 'O') oarray.push_back(number);			else barray.push_back(number);		}				int opos = 1;		int bpos = 1;		bool omoved=false;		bool bmoved=false;		int counter = 0;		while(1){			if(!barray.empty()){				if(barray.front() == bpos){					bmoved = false;				}				else{					if(barray.front() > bpos) bpos++;					else bpos--;					bmoved = true;				}			}			if(!oarray.empty()){				if(oarray.front() == opos){					omoved = false;				}				else{					if(oarray.front() > opos) opos++;					else opos--;					omoved = true;				}			}					if(moves.front()=='O'){				if(oarray.front()==opos && !omoved){					oarray.pop_front();					moves.pop_front();				}			}			else{				if(barray.front()==bpos && !bmoved){					barray.pop_front();					moves.pop_front();				}			}			counter++;			if(moves.empty()) break;		}									cout << "Case #" << i+1 << ": " << counter<<endl;	}				return 0;	}