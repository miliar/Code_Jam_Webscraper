#include <iostream>using namespace std;int task, ans, n, l, h, a[100];bool check(int x){	for (int i = 0; i < n; ++i)		if (a[i] % x != 0 && x % a[i] != 0)		return 0;	return 1;}int main(){	freopen("c.in", "r", stdin);	freopen("c.out", "w", stdout);		cin >> task;	for (int tt = 1; tt <= task; ++tt) {		cout << "Case #" << tt << ": ";		cin >> n >> l >> h;		for (int i = 0; i < n; ++i) cin >> a[i];		for (ans = l; ans <= h + 1; ++ans) if (check(ans)) break;		if (ans <= h) cout << ans << endl;		else cout << "NO" << endl;	}	return 0;}