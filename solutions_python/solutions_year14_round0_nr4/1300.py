#Google Code Jam 2014 Qualification Round Cimport copyt = input()for x in xrange(t):	print "Case #" + str(x + 1) + ":",	n = input()	naomi = sorted(map(float, raw_input().split()))	ken   = sorted(map(float, raw_input().split()))	naomi_copy = copy.deepcopy(naomi)	ken_copy = copy.deepcopy(ken)	y = 0	for i in xrange(n):		'''		if naomi[0] > ken[-1]:			y = y + n - i			break		if naomi[-1] < ken[0]: break		naomi.pop(0)		ken.pop()		'''		if naomi[0] > ken[0]:			y = y + 1			naomi.pop(0)			ken.pop(0)			continue		naomi.pop(0)		ken.pop()	print y,	z = 0	for i in xrange(n):		if naomi_copy[-1] < ken_copy[-1]:			naomi_copy.pop()			ken_copy.pop()			continue		naomi_copy.pop()		ken_copy.pop(0)		z = z + 1	print z