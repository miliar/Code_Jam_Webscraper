lines = []with open('input.txt','r') as f:   lines = [l.strip() for l in f.read().lower().splitlines() if len(l.strip()) > 0]   lines.pop(0)names = []for l in lines:   parts = l.split()   names.append((parts[0], int(parts[1])))def substrings(str):   length = len(str)   for start in xrange(length):      for end in xrange(start + 1, length + 1):         yield str[start:end]   def is_consonant(c):   return (c != 'a' and c != 'e' and c != 'i' and c != 'o' and c != 'u')      def min_consec_consonants(substring, n):   consonants_seen = 0   for char in substring:      if is_consonant(char):         consonants_seen += 1         if consonants_seen >= n:            return True      else:         consonants_seen = 0   return False      case = 1      for name, n in names:   n_values = 0   for s in substrings(name):      if min_consec_consonants(s, n):         n_values += 1   print "Case #%d: %d" % (case, n_values)   case += 1   